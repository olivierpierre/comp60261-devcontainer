#include <stdio.h>

int main() {
    int i = -42;
    float f = 12.34;
    char c = 'a';
    long unsigned int lui = 500;
    double d = 42.42;

    printf("%d, %f, %c, %lu, %lf\n", i, f, c, lui, d);
}